# Countdown Timer (Vite + React)

This is a minimal Vite + React project that implements a simple countdown timer.

Quick start

```bash
cd "/Users/palmerhudson/Desktop/Projects/React Course/Countdown-timer"
npm install
npm run dev
```

Open the local dev URL shown by Vite (usually http://localhost:5173).
